name = "breeze-webhook-relay"
main = "worker.js"
compatibility_date = "2024-09-01"

# Secrets (set via `wrangler secret put`):
#   BLA_DISPATCH_TOKEN  — GitHub PAT with `repo` scope on BreezeLauncherAndroid
#   WEBHOOK_SECRET      — random string, also configured as the webhook secret
#                         in BreezeLauncher repo Settings → Webhooks
